{
    "name": "rialo-weather-insurance",
    "version": "1.0.0",
    "description": "Parametric weather insurance platform - PayOnRain",
    "private": true,
    "workspaces": [
        "frontend-react",
        "prototype"
    ],
    "scripts": {
        "dev:local": "concurrently \"cd frontend-react && npm run dev\" \"cd prototype && npm start\"",
        "install:all": "npm install && cd frontend-react && npm install && cd ../prototype && npm install"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}